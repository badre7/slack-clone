"use client";

import { useState  } from "react";
import { SignInFlow } from "../types";

export const AuthScreen = () => {
const [state]

    return (
        <div>
            Auth screen 
        </div>
    );
};